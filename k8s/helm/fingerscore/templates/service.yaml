apiVersion: v1
kind: Service
metadata:
  name: {{ include "fingerscore.fullname" . }}
  labels:
    {{- include "fingerscore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fingerscore.selectorLabels" . | nindent 4 }}
